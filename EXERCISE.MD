## Mapbox token

This project requires a Mapbox public token.

Create a file named `.env.local` in the project root and add:

VITE_MAPBOX_TOKEN=pk.XXXXXXXXXXXXXXXX

(We’ll provide the token directly — no Mapbox account required.)
